{
  "name": "sentiment-api",
  "version": "1.0.0",
  "description": "Sentiment Analysis API with Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute sentiment-db --file=./schema.sql",
    "db:seed": "wrangler d1 execute sentiment-db --file=./seed.sql"
  },
  "keywords": ["sentiment", "api", "cloudflare", "workers"],
  "author": "Suiyang",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "wrangler": "^3.24.0",
    "typescript": "^5.3.3"
  }
}

